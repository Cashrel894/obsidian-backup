#Data8 
***第 p 百分位数(The pth percentile)***：大于等于全体数据中 x% 的数据的最小值。p 可取 0~100。
```python
percentile(70, sizes)
```

计算 p 百分位数的方法：
1. 将数据升序排序。
2. 计算 `k=(p/100)*n`。其中 n 为元素总量。
3. 如果 k 为整数，取排序后数据的第 k 个元素。
4. 否则，将 k 向上取整，取对应元素。

***下/中/上四分位数(The 1/2/3th quartile)***：分别指第 25/50/75 百分位数。